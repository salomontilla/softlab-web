---
import { getCollection } from "astro:content";
const proyectos = await getCollection('proyectos')
import Layout from "../layouts/Layout.astro";
import ProjectCard from "../components/ProjectCard.astro";

// Parámetros de paginación
const page = Number(Astro.url.searchParams.get('page')) || 1;
const itemsPerPage = 6;
const totalPages = Math.ceil(proyectos.length / itemsPerPage);
const start = (page - 1) * itemsPerPage;
const end = start + itemsPerPage;
const proyectosPaginados = proyectos.slice(start, end);
---
<Layout title="Productos">
    <section id="proyectos" class="w-full h-auto flex flex-col items-center justify-between">
        <header class="relative w-full h-fit px-8 md:px-28 py-8 bg-[#1B4583] flex flex-col text-amber-50 gap-y-2.5 justify-center items-center">
            <a class="absolute left-6 top-6 font-light text-base md:text-lg underline hover:scale-101 transition-all duration-300 ease-in-out cursor-pointer " href="/softlab-web/">← Volver</a>
            <h1 class="text-4xl md:text-5xl lg:text-6xl font-bold text-center">Productos</h1>
            <p class="text-xl md:text-2xl lg:text-3xl font-light text-center">Aqui ecuentras la  evidencia del compromiso, la creatividad y el talento de nuestros estudiantes en su proceso de formación profesional</p>
        </header>
        <div class="w-full px-10 md:px-14 lg:px-24 py-4 md:py-8 lg:py-12 flex flex-col justify-center items-center gap-8">
            <div class="w-full grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4">
                {proyectosPaginados
                    .map(proyecto => {
                      const { slug, data } = proyecto;
                      const autoresArray = Object.values(data.autores);
                      const tecnologiasArray = Object.entries(data.tecnologias);
                  
                      return (
                        <ProjectCard 
                          key={slug}
                          autores={autoresArray}
                          img_producto={data.img_producto}
                          nombre_producto={data.nombre_producto}
                          descripcion={data.descripcion}
                          fecha={data.fecha}
                          tecnologias={tecnologiasArray}
                          slug={slug}
                        />
                      );
                    })}
            </div>
            
        </div>
        
        <footer class="flex justify-center h-[80px] w-full">
          <nav aria-label="Page navigation example">
            <ul class="flex items-center -space-x-px h-10 text-base">
              <li>
                <a href={`?page=${page - 1}`} rel="external" class={`flex items-center justify-center px-4 h-10 ms-0 leading-tight border border-e-0 border-blue-300 rounded-s-lg ${page <= 1 ? 'text-gray-300 bg-gray-100 cursor-auto' : 'text-blue-600 bg-white hover:bg-blue-100 hover:text-blue-700'}`} aria-disabled={page <= 1}>
                  <span class="sr-only">Anterior</span>
                  <svg class="w-3 h-3" fill="none" viewBox="0 0 6 10"><path stroke="currentColor" stroke-width="2" d="M5 1 1 5l4 4"/></svg>
                </a>
              </li>
              {Array.from({ length: totalPages }).map((_, i) => {
                const pageNumber = i + 1;
                const isActive = pageNumber === page;
                return (
                  <li>
                    <a href={`?page=${pageNumber}`} rel="external" class={`flex items-center justify-center px-4 h-10 leading-tight border ${isActive ? 'text-white bg-blue-600 border-blue-600' : 'text-blue-600 bg-white border-blue-300 hover:bg-blue-100 hover:text-blue-700'}`}>
                      {pageNumber}
                    </a>
                  </li>
                );
              })}
                    
              <li>
                <a rel="external" href={`?page=${page + 1}`} class={`flex items-center justify-center px-4 h-10 leading-tight border border-blue-300 rounded-e-lg ${page >= totalPages ? 'text-gray-300 bg-gray-100 cursor-auto' : 'text-blue-600 bg-white hover:bg-blue-100 hover:text-blue-700'}`} aria-disabled={page >= totalPages}>
                  <span class="sr-only">Siguiente</span>
                  <svg class="w-3 h-3" fill="none" viewBox="0 0 6 10"><path stroke="currentColor" stroke-width="2" d="m1 9 4-4-4-4"/></svg>
                </a>
              </li>
            </ul>
          </nav>
        </footer>
        

    </section>
</Layout>