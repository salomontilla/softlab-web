---
const { imagen = "Default", autor="Default", titulo = "Default", contenido = "Default" } = Astro.props;

---

<div class="w-full max-w-sm md:max-w-md aspect-auto h-auto flex flex-col justify-evenly gap-3 p-8">
    <img src={imagen} alt="Imagen">
    <p class="text-amber-400 font-bold">{autor}</p>
    <h3 class="font-bold">{titulo}</h3>
    <p>{contenido}.</p>
    <button class="bg-[#1B4583] text-amber-50 rounded-2xl h-16 w-full flex justify-center items-center text-center transition-all duration-300 ease-in-out 
   hover:brightness-105 hover:scale-105 hover:shadow-lg 
   cursor-pointer active:scale-100 active:duration-75">
        Conoce más
    </button>
</div>